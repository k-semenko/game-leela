# Upstream remotes (historical)

Original private GitLab sources (archived / not shipped here):

- `apps/frontend` ← `psychological-game/pg_front`
- `apps/bff` ← `psychological-game/pg_bff`
- `apps/bot` ← `psychological-game/pg-bot`

Nested upstream `.git` directories were removed from this monorepo.
