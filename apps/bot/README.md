# `@game-leela/bot`

Telegram bot (NestJS + Telegraf) for Game Leela.

```bash
cp ../../.env.example ../../.env
npm ci
npm run start:dev
```

Requires `TG_BOT_TOKEN` in `.env`. See root [README.md](../../README.md).
